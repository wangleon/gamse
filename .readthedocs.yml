version: 2

sphinx:
    configuration: docs/conf.py

formats: all

python:
    version: 3.5
    install:
        - method: pip
          path: .
          extra_requirements:
              - sphix_catalystcloud_theme
